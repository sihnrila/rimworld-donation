# 서버 포트 (기본: 33210)
PORT=33210

# event.json 저장 경로 (기본: exe 옆 data/event.json)
EVENT_FILE_PATH=./data/event.json

# 이벤트 로그 경로
LOG_FILE_PATH=./data/events.log.jsonl

# Webhook 인증 시크릿 (변경 권장)
WEBHOOK_SECRET=change-me
